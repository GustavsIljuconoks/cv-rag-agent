export default function Home() {
  return (
    <main>
      <h1>CareerGraph</h1>
      <p>
        The development scaffold is running. Start by adding the backend health
        endpoint, then build the first vertical slice.
      </p>
    </main>
  );
}
